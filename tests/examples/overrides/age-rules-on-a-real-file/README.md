# Age rules on a real file

One topic's rules over a whole export.

Level:    simple
Input:    `examples/data/customers.csv` with `examples/rules/split_by_topic/01_age_rules.yaml`
Expected: the age rules applied across 49 rows
