# One rule file

A single rule file, named explicitly: rules are data the entry point points at.

Level:    simple
Input:    the demo frame with `examples/rules/split_by_topic/01_age_rules.yaml`
Expected: `AGE_NOT_INTEGER` enabled for the legacy rows the rule names
