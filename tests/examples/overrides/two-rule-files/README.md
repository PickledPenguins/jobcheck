# Two rule files

Rule files split by topic, loaded together; the order given is the precedence order.

Level:    moderate
Input:    the demo frame with `examples/rules/split_by_topic/01_age_rules.yaml` then `examples/rules/split_by_topic/02_email_rules.yaml`
Expected: both files' rules loaded, in the order given
