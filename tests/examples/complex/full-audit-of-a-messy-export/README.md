# Full audit of a messy export

Everything the entry point can print for one file, which is what an audit run looks like.

Level:    complex
Input:    `examples/data/customers.csv` with `examples/rules/split_by_topic/01_age_rules.yaml` and `examples/rules/split_by_topic/02_email_rules.yaml`, summarised
Expected: the registry, every failure, and the counts, in one run
