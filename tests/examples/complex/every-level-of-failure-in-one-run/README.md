# Every level of failure in one run

One file carrying one of every kind of problem the example checks look for.

Level:    complex
Input:    `examples/data/customers.csv`, summarised
Expected: presence failures, shape failures and range failures together, with the root cause of each row
