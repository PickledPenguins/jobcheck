# A clean file and a large one compared

The clean end of the scale in the same shape as the nightly job, so the two diff cleanly.

Level:    complex
Input:    `examples/data/customers_clean.csv` with all four rule files, summarised, as CSV
Expected: an empty CSV report and a full set of pass counts
