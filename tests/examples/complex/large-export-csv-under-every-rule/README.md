# A large export under every rule file

The largest thing the demo can be asked to do.

Level:    complex
Input:    `examples/data/customers_large.csv` with all four rule files, as CSV
Expected: a CSV report of 2,000 rows with every rule in force
