# A clean file, with every table

A passing file still has plenty to show: the registry, what each rule could
touch, and a summary where every count sits in `passed`.

Level:    moderate
Input:    `examples/data/customers_clean.csv`
Expected: registry tables, `No failures.`, and a summary of passes; exit 0
