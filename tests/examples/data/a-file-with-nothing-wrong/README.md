# A file with nothing wrong

What a passing run looks like. Worth having seen once, so an empty report is
read as an answer rather than as something having gone missing.

Level:    simple
Input:    `examples/data/customers_clean.csv` (24 valid rows)
Expected: `No failures.`; exit 0
