# A file with nothing wrong

What a clean file looks like, so the empty report is recognisable.

Level:    simple
Input:    `examples/data/customers_clean.csv` (24 clean rows)
Expected: `No failures.` under the report heading; exit 0
