# Explain a blank row

A row with nothing in it is the clearest demonstration of layering.

Level:    simple
Input:    row 21 of `examples/data/customers.csv`, which is entirely empty
Expected: every layer-0 check failed, everything below it skipped, and several root causes
