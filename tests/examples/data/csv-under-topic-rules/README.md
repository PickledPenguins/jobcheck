# CSV under topic rules

The format flag and the rule files are independent: either can change without the other.

Level:    moderate
Input:    `examples/data/customers.csv` with `examples/rules/split_by_topic/01_age_rules.yaml` and `examples/rules/split_by_topic/02_email_rules.yaml`
Expected: a CSV report reflecting both rule files
