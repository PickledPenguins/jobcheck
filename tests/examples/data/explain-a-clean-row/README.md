# Explain a row that passed

The explanation of a row with nothing wrong: every check evaluated, none failed,
and the last line says there is no root cause.

Level:    moderate
Input:    row 0 of `examples/data/customers_clean.csv`
Expected: every check `passed`, and `root cause: none - the row passed`
