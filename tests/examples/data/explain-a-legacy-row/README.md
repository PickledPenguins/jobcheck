# Explain a legacy row

The other direction: a rule turning a check on shows up as a check that evaluated.

Level:    moderate
Input:    row 4 of `examples/data/customers.csv` with `examples/rules/split_by_topic/01_age_rules.yaml`
Expected: a check enabled by a rule appearing in the explanation as having run
