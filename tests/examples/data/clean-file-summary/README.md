# Summary of a clean file

The summary of a file that passes: the shape to compare a bad run against.

Level:    simple
Input:    `examples/data/customers_clean.csv`
Expected: counts with nothing in the failed column, and no root causes
