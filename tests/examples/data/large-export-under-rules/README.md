# A large export under rules

Scale and rules together, which is what a nightly job actually runs.

Level:    moderate
Input:    `examples/data/customers_large.csv` with `examples/rules/from_another_directory/global_age_rule.yaml`
Expected: a summary over 2,000 rows with the global rule in force
