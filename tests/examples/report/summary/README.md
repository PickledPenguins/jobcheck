# Counts instead of lines

`--summary` adds the aggregate view under the report.

Level:    simple
Input:    the demo frame
Expected: the failure table, then per-check counts and the root cause of each failing row
