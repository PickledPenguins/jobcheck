# Explaining one row

Every test on row 4, in evaluation order, with why each one did or did not run.
The last line is the root cause.

Input:    `python3 examples/main.py --explain 4`
Expected: a per-test table ending in `root cause: ...`; exit 0
